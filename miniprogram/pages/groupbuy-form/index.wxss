/* 表单体系（行式 frow、条目列表 pair、吸底 footer-bar）来自 app.wxss，这里只放页面专属 */

/* 状态行的标题 + 说明（与管理端表单 frow-main 内的用法一致） */
.row-title {
  font-size: 27rpx;
  font-weight: 600;
}
.row-sub {
  font-size: 23rpx;
  color: var(--text-muted);
  margin-top: 6rpx;
}
