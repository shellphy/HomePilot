<view class="card ncard" bind:tap="goDetail">
  <view class="ncard-row">
    <view class="ncard-tag">公告</view>
    <view class="ncard-title">{{matter.title}}</view>
    <text class="ncard-date">{{matter.published_on}}</text>
  </view>
  <view class="ncard-body">{{matter.body}}</view>
</view>
